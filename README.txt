Edward Zhou, Nathan Johnson

Program:
It's a Notebook, there are no inputs necessary, all data is collected from the web and is processed then ran.
Frequent item sets can be discovered with the `apriori` function.
Association rules can be discovered with `make_output`, which gets the assocation rules and formats the output.